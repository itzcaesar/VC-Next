# Optional dependencies for explicit Fairseq HuBERT feature extraction.
#
# Fairseq 0.12.2 declares legacy Hydra/OmegaConf upper bounds that conflict
# with Python 3.11.  setup-runtime.ps1 installs fairseq itself with --no-deps,
# then installs this compatible runtime set.  The loader applies a narrow
# import-time shim for Fairseq's Python 3.11 dataclasses and registry code.
bitarray>=2.9
cffi>=1.15
cython>=3.0
hydra-core==1.3.2
omegaconf==2.3.0
regex>=2024.0
sacrebleu>=2.4
tqdm>=4.66
